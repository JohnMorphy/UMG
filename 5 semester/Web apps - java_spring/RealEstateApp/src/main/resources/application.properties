# parametry poziomu logowania/raportowania aplikacji
spring.sql.init.mode=always
spring.sql.init.platform=postgres

spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.mode=HTML

# connect through tunnel
spring.jpa.properties.hibernate.default_schema=spring_realestateapp
spring.datasource.url=jdbc:postgresql://localhost:5432/s48432
spring.datasource.username=s48432
spring.datasource.password=

# to jest dla wara
server.servlet.context-path=/RealEstateApp/

# parametry dla JPA
spring.jpa.hibernate.ddl-auto=update